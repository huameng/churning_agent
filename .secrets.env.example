# Copy to .secrets.env (gitignored) and fill in. Per-site portal credentials.
# Keys are <SITE>_EMAIL / <SITE>_PASSWORD, matching a site's credential_key in
# tools/sites.py. Only whitelisted sites are ever driven.
TOPCASHBACK_EMAIL=you@example.com
TOPCASHBACK_PASSWORD=your-password

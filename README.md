Agentic decision maker which reads blogposts from Doctor of Credit and decides what to do with them.

This project is intended to run with Google ADK -- all other entrypoints are secondary.

Start usage by
uv install
and then
uv run adk web src

Then navigate to localhost:8000 and interact with the agent:
"Show me moneymaking offers"
